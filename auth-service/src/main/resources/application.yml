server:
  port: 8081
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/  
    register-with-eureka: true
    fetch-registry: true
  instance:
    hostname: auth-service

spring:
  application:
    name: auth-service
  data:
    mongodb:
      uri: mongodb+srv://movie-api:1234@movie-api.ufpbclf.mongodb.net/?retryWrites=true&w=majority&appName=auth-service
      database: student-mang-sys

